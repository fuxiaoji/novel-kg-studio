[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "novel-kg-studio"
version = "0.1.0"
description = "Two-pass LLM knowledge-graph building + graph/lexical RAG + 3D dynamic visualization for long novels"
requires-python = ">=3.10"
dependencies = [
  "openai",
  "networkx",
  "numpy",
  "pandas",
  "plotly",
  "matplotlib",
  "scipy",
  "pyyaml",
  "json-repair",
]

[project.optional-dependencies]
dev = ["pytest"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
